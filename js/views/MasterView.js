/*global define*/
define([
    'jquery',
    'underscore',
    'backbone'
    
], function(
    $,
    _,
    Backbone
) {
    'use strict';

    var MasterView = Backbone.View.extend({
        intialize: function(options) {
        }
    });

    return MasterView;
});
